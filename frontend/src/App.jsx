import AppRoutes from "./routes/AppRoutes";

/*
  App.jsx is the root component of the project.

  We keep it simple.
  The actual page switching logic is handled inside AppRoutes.
*/

export default function App() {
  return <AppRoutes />;
}